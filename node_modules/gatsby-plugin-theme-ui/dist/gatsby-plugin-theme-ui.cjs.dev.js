'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var utils = require('@theme-ui/css/utils');

// add colors, fonts, fontSizes, space, etc
const theme = utils.makeTheme({});

exports["default"] = theme;
